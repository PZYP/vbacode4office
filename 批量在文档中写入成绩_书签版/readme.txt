说明：此code可以将指定文件夹下文档中的指定位置（用插入书签标注）批量写入指定Excel表中的数据；
支持doc/docx文档；
支持文档名称是1_张三、1-张三、张三_1、张三-1，这种带分隔符的情形；
代码执行流程：指定文件夹>查找Excel姓名>找到姓名以及姓名的文档>查找对应成绩列单元格>插入到文档对应的书签位置；

运行环境：
Windows7/64bit含以上

数据清洗：
要求doc/docx文档，必须预先已经在对应的位置插入书签，并且设置了对应的书签名称。

运行前微调：
1、必须安装wps；
2、code中修改文档文件夹路径；
3、code中修改对应Excel中的姓名列（列转换成数字，比如A列=1、B列=2……）；
4、code中修改Excel成绩列与文档指定成绩填充位置的书签名对应关系；
    例如：
    语文成绩 C列(转换成数3) 对应 文档中的书签“语文成绩”
    数学成绩 D列(转换成数4) 对应 文档中的书签“数学成绩”
    英语成绩 E列(转换成数5) 对应 文档中的书签“英语成绩”
    代码中示例：
    语文成绩 = ws.Cells(foundRow, 3).Value
    数学成绩 = ws.Cells(foundRow, 4).Value
    英语成绩 = ws.Cells(foundRow, 5).Value
    ……
    Call 写入书签(wordDoc, "语文成绩", 语文成绩)
    Call 写入书签(wordDoc, "数学成绩", 数学成绩)
    Call 写入书签(wordDoc, "英语成绩", 英语成绩)
5、code应该在Excel成绩表中创建；

备注：
无