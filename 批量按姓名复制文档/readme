说明：此code可以基于Excel表格中姓名列，批量在指定文件夹中查找并复制对应姓名的文档到新的文件夹中；
支持doc/docx文档；

运行环境：
Windows7/64bit含以上

数据清洗：
无要求。

运行前微调：
1、必须安装wps；
2、code中修改Excel表姓名所在列；
3、code中修改文档集所在的文件夹路径；
4、code中修改拷贝符合姓名的文档到新的文件夹路径；
5、code应该在Excel中创建；

备注：
如果Excel表格中有重复的姓名（但实际上是2个不同的人员），该脚本无法区分同名同姓的不容人员；
则结果会出现输出文件夹中存在 张三.docx\张三_重复.docx，需要人工介入处理。