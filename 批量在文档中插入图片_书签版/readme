说明：此code可以基于文档的名称，检索对应名称的照片，然后插入到指定的书签名称位置处；
支持doc/docx文档；

运行环境：
Windows7/64bit含以上

数据清洗：
必须预先在每个文档中对应位置插入书签，书签名称比如“个人照片”。

运行前微调：
1、必须安装wps；
2、code中修改书签名称；
3、无需填写路径（运行时会弹窗提示选择文件夹）；
4、code应该在新建doc/docx文档中创建；

备注：

该脚本无法区分同名同姓的不容人员；如有同名同姓，需要提前人工介入处理。