说明：此code可以基于文档的名称，检索对应名称的照片，然后插入到指定的占位词位置处（并自动删除占位词）；
支持doc/docx文档；

运行环境：
Windows7/64bit含以上

数据清洗：
必须预先在每个文档中填入相同的占位词，比如“1寸红底照片”。

运行前微调：
1、必须安装wps；
2、code中修改占位词；
3、无需填写路径（运行时会弹窗提示选择文件夹）；
4、code应该在新建doc/docx文档中创建；

备注：
该脚本无法区分同名同姓的不容人员；如有同名同姓，需要提前人工介入处理。